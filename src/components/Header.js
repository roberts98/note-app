import React from 'react'

const Header = () => (

  <header className="header">
    <div className="container">
      <h1 className="header__title">Save your notes</h1>
    </div>
  </header>
);

export default Header;